spring:
     datasource:
          url: jdbc:mysql://db:3306/monitoring_system?useLegacyDatetimeCode=false&serverTimezone=Europe/Prague&allowPublicKeyRetrieval=true&useSSL=false
          username: root
          password: ''
          driver-class-name: com.mysql.cj.jdbc.Driver
     jpa:
          database-platform: org.hibernate.dialect.MySQL5Dialect
          generate-ddl: true
          hibernate:
               ddl-auto: update
          properties:
               hibernate:
                    dialect: org.hibernate.dialect.MySQL5Dialect
hibernate:
     hbm2ddl:
          auto: update